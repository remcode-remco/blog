<!DOCTYPE html>
<html>
  <head>
    <title>Blogzoo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="w-full">
      <div class=' text-xl text-white bg-black flex justify-between p-3'>
        <%= link_to root_path do %>
          <i class="fa-solid fa-house"></i>          
        <% end %>
        <% if current_user %>
          <div class='flex items-center gap-3'>
            <%= link_to "New post", new_post_path, class: "rounded-lg px-3 bg-green-600 text-white font-medium" %>
            <%= button_to 'Logout', sign_out_path, method: :delete, data: { turbo: false } %>
          </div>
        <% else %>
          <%= button_to "Sign in with Google", "/auth/google_oauth2", data: { turbo: false } %>
        <% end %>
      </div>

    <%= yield %>

    
    <footer class='w-full text-md text-white bg-black p-3 text-center'>
      blog v1 - 2024
    </footer>
    </main>
  </body>
</html>
